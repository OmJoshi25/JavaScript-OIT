// function volume(r,h){
//     let vol = 3.12 * sq(r) * h;
//     return vol;
// }

// function sq(r){
//     let sq = r**2;
//     return sq;
// }

// let r=Number(prompt("Enter radius "));
// let h=Number(prompt("Enter height "));
// console.log("Volume",volume(r,h).toFixed(2));//fixed value upto 2 decimal places after point

//Arrow Function
// let calVol = (r,h) => {
//     let vol = 3.12 * sqr(r) * h;
//     return vol;
// }

//let calVol = (r,h) => 3.12 *sqr(r) * h;

//let sqr = (r) => Number(2**2) //returns r**2

//let sqr = r => r**2 //if only one parameter then o need of brackets and if it does not have any parameters then only ()

//let vol=calVol(r,h);